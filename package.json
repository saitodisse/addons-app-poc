{
  "name": "addons-poc",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r --filter './packages/**' test",
    "dev": "node scripts/dev-all.mjs --open",
    "dev:host": "pnpm --filter @addons/host-app dev",
    "dev:addons": "pnpm -r --parallel --filter './packages/addon-text-*' serve",
    "dev:hello": "pnpm --filter @addons/addon-hello serve",
    "kill-all": "node scripts/kill-all.mjs",
    "build:host": "pnpm --filter @addons/host-app build"
  }
}
